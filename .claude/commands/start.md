---
description: Start-of-session kickoff — get Claude up to speed
---

Get up to speed for this session:

1. Read **CLAUDE.md** (working conventions) and the most recent
   **SESSION_HANDOFF_*.md** (the in-flight snapshot from last session).
2. Read **ROADMAP.md** — the **NOW / NEXT / LATER** priority block + the epic
   list (incl. **Epic 10 Lumé/AI**). This is now a standing session-start read,
   not just for scoping: it's where direction + feature-threads live. (Skim the
   full epic bodies only when scoping new direction.)
3. Read **BUGS.md** — the **Open** entries + the **Epic assignment** table at the
   top (which open items are defects-that-stay vs feature-threads flagged to
   graduate into ROADMAP).
4. Recall relevant memories.
5. Tell me back, briefly:
   - where the last session left off (from the handoff),
   - the current **NOW / NEXT** from ROADMAP,
   - open **defects** from BUGS (by ID + one-liner), and any **feature-threads**
     flagged to graduate to an epic,
   - the one work item you understand we're starting,
   - the branch you'll create before editing.

Don't start editing `main` — branch first. Coach me as we go: if I stack a
new request mid-work, flag it and propose finishing the current item first.
Remember the `Bug:` routing — defects → BUGS, feature/capability/design
threads → ROADMAP under their epic.
